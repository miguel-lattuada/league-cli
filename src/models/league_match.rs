pub struct LeagueMatch {
    pub platform_id: String,
    pub game_id: u64,
    pub champion: u64,
    pub queue: u64,
    pub season: u64,
    pub timestamp: u64,
    pub role: String,
    pub lane: String,
}
